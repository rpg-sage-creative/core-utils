/**
 * Puts quotes around a value.
 * If the quotes used are in the value, they are escaped.
 * Default style: "double"
 */
export declare function quote(value: string, style?: "double" | "single"): string;
